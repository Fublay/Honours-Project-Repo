"""Pipeline helpers for collecting and preparing live trial data.

A "pipeline" here means the ordered steps that read serial lines, parse them,
and return clean arrays that scoring/optimization code can use.
"""
